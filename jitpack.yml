# jitpack.yml
jdk:
  - openjdk17

android:
  build: true

gradle:
  version: 8.0.2 # Update this to match your project's Gradle wrapper version

install:
  - chmod +x gradlew # Ensure gradlew is executable
  - ./gradlew clean assembleRelease
